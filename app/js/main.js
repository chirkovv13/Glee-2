$ (function () {
  
  $('.top-slider').slick({
    arrows: false,
    dots: true,
    // autoplay: true,
    fade: true
  })



  var mixer = mixitup('.week-filter__list');
  var mixer = mixitup('.new-filter__list');

});